# largest-banks-etl
ETL pipeline that scrapes data on the world's largest banks by market capitalisation, transforms currency values using exchange rates, and stores the results in CSV and SQLite for querying.
